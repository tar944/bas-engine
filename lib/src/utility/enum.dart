enum PageType { mainPage, subPage, dialog, menu, submenu, rightMenu }

enum PartType {
  menuBar,
  statusBar,
  optionBox,
  toolsBar,
  toolsPallet,
  menu,
  submenu,
  rightClick,
  tabBar,
  tabBody
}

enum ObjectType {
  checkbox,
  label,
  button,
  radioButton,
  icon,
  selectBox,
  table,
  chart,
  menu,
  menuItem,
  cell,
  editBox,
  textBox,
  richEditBox
}

enum ImageStatus { finished, created }

enum ActionKind {
  click,
  doubleClick,
  wheelDown,
  wheelUp,
  rightClick,
  slideRight,
  slideLeft,
  scrollHorizontal,
  scrollVertical,
  type,
  drag,
  hold,
  typeNumber
}
