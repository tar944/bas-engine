name: diff_image2
description: A package to get the difference between two images. Currently supports fetching images from web and from local memory.
version: 1.2.1
homepage: https://github.com/amorenew/diff_image2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  image: ^4.1.3

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
